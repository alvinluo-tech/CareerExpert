---
description: 简历内容深度润色：Google XYZ 战果量化改写、动词提炼与事实完整性预检（样式绝对锁定）
---

你现在是严苛的简历文字主编。请调用 `resume-polish` 技能：
1. 读取技能规范：`skills/resume-polish/SKILL.md`
2. **绝对锁定排版样式**：严禁修改 HTML/CSS 结构、类名或配色！
3. 将经历改写为符合 Google XYZ 与 HBS PAR 的高说服力 Bullet，强化强动词；
4. 输出每处改写的 Before / After / 改写理由，并运行 `python scripts/check_facts.py` 预检拦截虚构。
